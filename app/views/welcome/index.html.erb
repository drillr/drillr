  <span class="drill">
    <p>DRILL<span id="r">R</span></p>
  </span>

  <div class="app-title">
    <p>Drop down and give me 20, private!</p>
  </div>


  <div class="form-position">

    <div class="login-form">

      <!-- <h2 id="login-yell">Log in, Private!</h2> -->
        <% if !user_signed_in? %>
          <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <%= f.input :email, autofocus: true %>
            <%= f.input :password %>
            <%= f.submit "Log in",  class: "btn btn-primary" %>
          <% end %>
        <% end %>
    <br/>
  <%= link_to "Register", new_user_registration_path, class: "btn btn-primary register" %>
    </div>
  </div>
  <div class="view-drills-button">
  <%= link_to "See Drills >>", categories_path %>
</div>
